# Servidor
PORT=8080
GIN_MODE=debug

# Base de datos (para Fase 1B)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=uniflowdb

# Autenticación (Delegada a API Management)
# NOTA: Este servicio NO genera ni valida tokens JWT localmente.
# La autenticación se realiza vía Google OAuth en Azure API Management,
# que inyecta los siguientes headers en cada request:
#   - X-User-ID (obligatorio)
#   - X-User-Email (opcional)
#   - X-User-Name (opcional)
#   - X-User-Picture (opcional)
#
# Para desarrollo local (GIN_MODE=debug), puedes usar headers X-Dev-User-*
# para simular autenticación sin pasar por API Management.

# Logging (opcional)
LOG_LEVEL=info
LOG_FORMAT=json

DEFAULT_TZ=America/Costa_Rica
PAGE_SIZE=10